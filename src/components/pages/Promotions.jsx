import React from "react";

import StyleElements from "../../styles/elements.style";

export default (props) => (
  <section style={props}>
    <div style={props}></div>
    <div style={props}></div>
  </section>
)